<snippet>
	<content><![CDATA[
auto it = upper_bound (v.begin(), v.end(), make_pair(val - 1,inf), []( pair<ll, ll> const& a, pair<ll, ll> const& b ) { return a.first < b.first; }); // acc to first
]]></content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>pair_upper_bound</tabTrigger>
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<!-- <scope>source.python</scope> -->
</snippet>
